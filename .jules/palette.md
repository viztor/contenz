## 2024-05-17 - Add CLI output color highlighting
**Learning:** CLI output without syntax highlighting makes it hard to distinguish structural keys from values and critical error diagnostics from plain text.
**Action:** Used `picocolors` to add semantic color to keys, values, and error messages in the `@contenz/cli` output.
