## 2024-05-15 - Add picocolors to cli output
**Learning:** Adding colored output to CLI commands makes the output much easier to read and scan, especially for structured data like JSON or deeply nested objects. It provides semantic grouping.
**Action:** Use `picocolors` to add colors to the `prettyPrint` function in `@contenz/cli` output utility.
